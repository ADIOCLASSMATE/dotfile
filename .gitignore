# --- shell local overrides ---
zsh/zshrc.d/99-private.local.zsh
zsh/zshrc.d/*.local.zsh

# --- system / editor files ---
.DS_Store
*.swp
*.swo
*~
*.bak
*.orig
*.rej
.idea/
.vscode/
Session.vim
tags

# --- .claude (runtime-only, not tracked) ---
.claude/

# --- .codex (runtime-only, not tracked) ---
.codex/

# --- .agents (runtime-only, not tracked) ---
.agents/

# --- pipeline state ---
.pipeline/

# --- other local files ---
.sii/
responses_proxy.py
